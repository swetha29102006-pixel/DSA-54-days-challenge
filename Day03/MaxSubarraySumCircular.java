class Solution {
    public int maxSubarraySumCircular(int[] nums) {
        int currMin = nums[0];
        int currMax = nums[0];
        int minSum = nums[0];
        int maxSum = nums[0];

        return 0;
    }
}
