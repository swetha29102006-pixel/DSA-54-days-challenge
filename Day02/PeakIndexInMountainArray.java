class Solution 
{
    public int peakIndexInMountainArray(int[] arr) 
    {
        int low = 0;
        int high = arr.length;
        int len = high;
        high--;

        while (low <= high)
        {
            int mid = low + (high - low) / 2;
        }

        return 1;
    }
}
